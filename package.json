{
  "name": "multi-kiew",
  "version": "1.0.0",
  "description": "Telegram bot",
  "main": "src/main.js",
  "scripts": {
    "build": "npm install",
    "start": "bash -c 'source .env && npm run build && node --experimental-modules src/main.mjs'"
  },
  "author": "ggoncalves",
  "license": "MIT",
  "dependencies": {
    "request": "^2.83.0",
    "roll": "^1.2.0",
    "slimbot": "^5.1.0"
  },
  "devDependencies": {
    "prettier": "3.1.1"
  }
}
